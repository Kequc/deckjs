// Generated by CoffeeScript 1.9.3
(function() {
  var Deck,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  Deck = (function() {
    Deck.prototype.SUITS = ['spades', 'hearts', 'diamonds', 'clubs'];

    Deck.prototype.RANKS = ['ace', 'king', 'queen', 'jack', '10', '9', '8', '7', '6', '5', '4', '3', '2'];

    function Deck(opt) {
      var base, base1;
      this.opt = opt != null ? opt : {};
      this.inlay = bind(this.inlay, this);
      this.draw = bind(this.draw, this);
      this.count = bind(this.count, this);
      this.shuffle = bind(this.shuffle, this);
      if ((base = this.opt).extend == null) {
        base.extend = [];
      }
      if ((base1 = this.opt).multiply == null) {
        base1.multiply = 1;
      }
      if (this.opt.multiply < 1) {
        this.opt.multiply = 1;
      }
      this.shuffle();
    }

    Deck.prototype.shuffle = function() {
      var card, i, j, k, l, len, len1, len2, m, rank, ref, ref1, ref2, ref3, suit;
      this.cards = [];
      for (i = j = 1, ref = this.opt.multiply; 1 <= ref ? j <= ref : j >= ref; i = 1 <= ref ? ++j : --j) {
        ref1 = this.SUITS;
        for (k = 0, len = ref1.length; k < len; k++) {
          suit = ref1[k];
          ref2 = this.RANKS;
          for (l = 0, len1 = ref2.length; l < len1; l++) {
            rank = ref2[l];
            this.inlay({
              suit: suit,
              rank: rank
            });
          }
        }
        ref3 = this.opt.extend;
        for (m = 0, len2 = ref3.length; m < len2; m++) {
          card = ref3[m];
          if (!(card.limit && i > card.limit)) {
            this.inlay(card);
          }
        }
      }
      return true;
    };

    Deck.prototype.count = function() {
      return this.cards.length;
    };

    Deck.prototype.draw = function() {
      var count;
      count = this.count();
      if (count > 0) {
        return this.cards.splice(Math.floor(Math.random() * count), 1)[0] || null;
      } else {
        return null;
      }
    };

    Deck.prototype.inlay = function(card) {
      if (card == null) {
        card = {};
      }
      if (card.suit && card.rank) {
        card = {
          suit: card.suit,
          rank: card.rank
        };
        this.cards.push(card);
        return card;
      } else {
        return false;
      }
    };

    return Deck;

  })();

  if (typeof module === "object" && typeof module.exports === "object") {
    module.exports = Deck;
  } else {
    window.Deck = Deck;
  }

}).call(this);
